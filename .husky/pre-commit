#!/bin/sh

# Run existing pre-commit tasks
npx lint-staged

# Security: Scan for secrets using gitleaks (if installed)
if command -v gitleaks &> /dev/null; then
  echo "🔍 Scanning for secrets..."
  if ! gitleaks detect --source . --verbose --no-banner; then
    echo "❌ Secret detected! Commit blocked."
    echo "📝 Review the files and remove any hardcoded secrets."
    echo "💡 Use environment variables instead."
    exit 1
  fi
  echo "✅ No secrets detected"
else
  echo "⚠️  Gitleaks not installed. Install with: brew install gitleaks"
  echo "   Or visit: https://github.com/gitleaks/gitleaks#installation"
fi

# Security: Check for common security patterns
echo "🔍 Checking for security anti-patterns..."

# Check for hardcoded JWT patterns (excluding documentation)
non_md_files=$(git diff --cached --name-only | grep -v "\.md$" || true)
if [ -n "$non_md_files" ]; then
    if problematic_files=$(echo "$non_md_files" | xargs grep -l -E "eyJ[A-Za-z0-9+/]{20,}={0,2}" 2>/dev/null) && [ -n "$problematic_files" ]; then
        echo "❌ Potential hardcoded JWT/token detected!"
        echo "   Use environment variables for all tokens and keys."
        echo "   Found in files:"
        echo "$problematic_files" | sed 's/^/   - /'
        exit 1
    fi
fi

# Check for console.log with potential sensitive data
js_ts_files=$(git diff --cached --name-only --diff-filter=ACM | grep -E "\.(ts|tsx|js|jsx)$" || true)
if [ -n "$js_ts_files" ]; then
    if problematic_files=$(echo "$js_ts_files" | xargs grep -l -E "console\.(log|error|warn|info).*\b(password|token|secret|key|credential)" 2>/dev/null) && [ -n "$problematic_files" ]; then
        echo "⚠️  Warning: console.log with potentially sensitive variable names detected"
        echo "   Make sure you're not logging sensitive information"
        echo "$problematic_files" | sed 's/^/   - /'
    fi
fi

echo "✅ Security checks passed"
